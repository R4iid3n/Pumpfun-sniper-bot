# Core dependencies for Pump.fun Sniper Bot
# For paper trading and live trading

# Async and networking
websockets>=11.0
requests>=2.31.0
aioquic>=1.0.0       # Direct QUIC TPU send (use_tpu_direct)

# Solana blockchain interaction (includes SPL token support)
solana>=0.30.2
solders>=0.21.0,<0.27
anchorpy>=0.18.0

# Base58 encoding for wallet keys
base58>=2.1.1

# Additional utilities
python-dateutil>=2.8.0

# Note: tkinter comes with Python (no pip install needed)
# Note: asyncio, base64, struct are Python built-ins (no pip install needed)
